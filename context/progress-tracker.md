# Progress Tracker

Update this file after every meaningful implementation
change.

## Current Phase

- `11-base-canvas.md` implemented (`components/editor/canvas-room.tsx` wraps `LiveblocksProvider`/`RoomProvider`/`ErrorBoundary`/`ClientSideSuspense`; `components/editor/canvas.tsx` wires `useLiveblocksFlow` into `ReactFlow` with loose connections, `fitView`, `MiniMap`, dot background, and live cursors; `types/canvas.ts` holds the shared `CanvasNode`/`CanvasEdge`/`NODE_COLORS`/`NODE_SHAPES` types; static checks and the default Turbopack production build pass).

## Current Goal

- The collaborative canvas foundation is in place but unverified against a real room connection — no `LIVEBLOCKS_SECRET_KEY` exists yet (see Open Questions, carried over from `10-liveblocks-set.md`). Next feature-spec work is custom node/edge rendering (node color/shape palette per `ui-context.md#canvas`) and persistence, both explicitly out of scope for `11-base-canvas.md`. Design-token migration (colors) is complete; radius-scale tokens remain deferred (see Open Questions).

## Completed

- Installed and configured `shadcn/ui` (`npx shadcn@latest init -d`) — preset `base-nova`, component library `@base-ui/react`, CSS variables theming.
- Added shadcn components: Button, Card, Dialog, Input, Tabs, Textarea, ScrollArea (`components/ui/*`).
- Installed `lucide-react`.
- Created `lib/utils.ts` with `cn()` helper (clsx + tailwind-merge).
- Forced dark theme by default by adding `dark` class to `<html>` in `app/layout.tsx`, since `app/globals.css` had no existing theme to match (CNA defaults had been stripped) and the spec requires no default light styling.
- Verified: `tsc --noEmit` clean, `next lint` clean, `next build` succeeds, dev server renders `<html>` with `dark` class applied.
- Built `components/editor/editor-navbar.tsx` — fixed h-12 top bar, left/center/right sections, sidebar toggle button swapping `PanelLeftOpen`/`PanelLeftClose` based on an `isSidebarOpen` prop, dark background with bottom border via existing `bg-background`/base-layer border tokens.
- Built `components/editor/project-sidebar.tsx` — `fixed` panel (not in flex flow), slides in/out via `translate-x` transition driven by an `isOpen` prop, header with "Projects" title + close button, shadcn `Tabs` (My Projects / Shared) each with an empty placeholder state, full-width "New Project" button with `Plus` icon pinned to the bottom. (Originally it never pushed canvas content on any breakpoint; a later `08-editor-workspace-shell.md` follow-up added a matching `md:`+ margin on `<main>` so it now pushes/re-centers the canvas like the AI panel does — see that entry in Completed. Below `md` it's still a pure overlay with no push, by design.)
- Built `components/editor/editor-dialog.tsx` — reusable pattern wrapping the existing `components/ui/dialog.tsx` primitives (title, description, footer actions), styled entirely from the tokens already in `dialog.tsx`/`global.css`. Not wired to any trigger yet, per spec ("do not build actual dialogs yet").
- Added `app/editor/page.tsx` (client component) wiring `EditorNavbar` + `ProjectSidebar` together with local `useState` so the toggle behavior is real and testable — this is the minimal "editor screen" the spec says these components frame.
- Verified end to end with a headless-browser pass (Playwright, since `chromium-cli` wasn't available in this environment): toggle button icon swaps, sidebar slides in without shifting the canvas, tab switching shows the correct empty state, closing returns to initial state, zero console/page errors. Screenshots + driver script left in the session scratchpad only (not committed).
- Verified: `tsc --noEmit` clean, `npm run lint` clean, `npm run build` succeeds (routes `/`, `/editor` both static).
- Implemented 03-auth.md end to end:
  - `proxy.ts` — switched from `clerkMiddleware()` (no-op) to protected-first: `createRouteMatcher` built from `NEXT_PUBLIC_CLERK_SIGN_IN_URL`/`NEXT_PUBLIC_CLERK_SIGN_UP_URL` marks only those two paths public, `auth.protect()` runs on everything else. Kept the existing static-asset-excluding `matcher` config as is.
  - `app/layout.tsx` — `ClerkProvider` now uses the `dark` theme from `@clerk/ui/themes` (was `shadcn`) with an explicit `variables` block mapping every Clerk color/radius token (`colorBackground`, `colorForeground`, `colorPrimary`, `colorPrimaryForeground`, `colorNeutral`, `colorMuted(Foreground)`, `colorInput(Foreground)`, `colorBorder`, `colorRing`, `colorDanger`, `borderRadius`) to the matching `var(--*)` CSS custom property already defined in `globals.css` — no hardcoded colors.
  - Removed the now-unused `@import "@clerk/ui/themes/shadcn.css"` from `globals.css` (that Tailwind `@source` scan was specific to the `shadcn` theme variant, which is no longer used).
  - `components/auth/auth-layout.tsx` (new) — shared two-panel shell: left panel (`hidden lg:flex`, so mobile is form-only) with compact `Ghost` lucide logo, one-line tagline, and a plain-text feature list (three lines, `Check` icon prefix, no cards); right panel centers `children`. No gradients, no hero, no scroll.
  - `app/sign-in/[[...sign-in]]/page.tsx` and `app/sign-up/[[...sign-up]]/page.tsx` — now wrap `<SignIn />`/`<SignUp />` in `AuthLayout`.
  - `app/page.tsx` — now a Server Component: `await auth()` then `redirect("/editor")` if `isAuthenticated`, else `redirect("/sign-in")`. Dropped the old client-rendered sign-in/sign-up buttons.
  - `components/editor/editor-navbar.tsx` — since `/editor` is now fully behind proxy protection, dropped the `Show`/`SignInButton`/`SignUpButton` signed-out branch; right section renders Clerk's `UserButton` unconditionally.
  - `.env.local` already had all required Clerk env vars (publishable key, secret key, sign-in/up URLs, fallback redirect URLs) and `@clerk/ui` was already in `package.json` — no new install needed.
- Verified with a real headless-browser pass (Playwright via `npx`, driving the already-running `next dev` on :3000): `/` → redirects to `/sign-in` when signed out; `/sign-in` and `/sign-up` render the two-panel layout at 1440×900 and collapse to form-only at 390×844; dark theme reads correctly from the CSS variables (matches app surface/border colors, not stock Clerk `shadcn` colors); zero console errors on any of the four screenshots.
- Verified: `npm run build` succeeds (`/` and the two catch-all auth routes are dynamic as expected since they call `auth()`/render `<SignIn>`/`<SignUp>`; `/editor` still static), `npm run lint` clean.
- Filled in the five still-templated context files from direct codebase inspection (not invented):
  - `architecture.md` — real stack table, system boundaries (`app/`, `proxy.ts`, `components/{ui,editor,auth}/`, `lib/`), storage model (none yet), auth/access model (Clerk + protected-first `proxy.ts` + `/`'s explicit redirect), and four invariants.
  - `ui-context.md` — full dark-theme color table pulled straight from `app/globals.css`'s `.dark` block, typography (Geist Sans/Mono), the `--radius-*` scale and its formula, component library conventions, the three layout patterns actually built (editor, auth, modal), and icon sizing observed in the code.
  - `code-standards.md` — concretized every bracketed rule against what's actually true (strict TS, `@/*` alias, Server-Components-by-default, `proxy.ts`-only auth, CSS-variable-only styling, `components/ui/*` regenerate-don't-edit), left API Routes / Data & Storage explicitly unfilled since neither exists yet.
  - `ai-workflow-rules.md` — kept the already-correct generic scaffolding, made the examples concrete (numbered feature-spec files, `components/ui/*` as the protected path, `node_modules/next/dist/docs/` as a protected reference path).
  - `project-overview.md` — deliberately did **not** invent Goals, full Feature scope, Out of Scope, or Success Criteria, since no spec defines them; filled in only what's observably true (Clerk-gated editor shell, project sidebar with empty placeholder tabs, canvas placeholder) and left the rest explicitly marked as needing product input, per `ai-workflow-rules.md`'s own "don't invent product behavior" rule.
- Replaced all five of the above with the user's real, hand-written product spec (authored directly in the Downloads backup, then copied in verbatim):
  - `project-overview.md` — copied as-is: real-time collaborative system-design workspace, AI-generated architecture from a prompt onto a shared Liveblocks/React Flow canvas, spec generation to Markdown. No changes needed.
  - `architecture.md` — copied from their `architecture-context.md` content. Kept the in-repo filename as `architecture.md` (not `architecture-context.md`) to match `AGENTS.md`'s existing repo-relative read order rather than editing `AGENTS.md`.
  - `code-standards.md`, `ai-workflow-rules.md` — copied as-is, except fixed one mechanical bug: both files cross-reference `architecture-context.md`, which doesn't exist under that name in the repo (see above) — repointed both references to `architecture.md`.
  - `ui-context.md` — copied as-is.
  - Did **not** edit their prose to add things they didn't write (e.g. didn't insert `components/editor/`/`components/auth/` into `architecture.md`'s System Boundaries list even though those folders now exist) — flagged as an open question instead of silently rewriting their spec.
- Design-token migration (colors — see `ui-context.md#colors`):
  - `app/globals.css` — added the raw palette (`--bg-base`, `--bg-surface`, `--bg-elevated`, `--bg-subtle`, `--border-default`, `--border-subtle`, `--text-primary/secondary/muted/faint`, `--accent-primary` + `-dim`, `--accent-ai` + `-text`, `--state-error/success/warning`) and collapsed the old `:root`/`.dark` split into one `:root, .dark { }` block with identical values (the app is dark-only, so a separate light palette was dead code — see `ui-context.md#theme`). Left `--chart-1..5` on their original placeholder `oklch()` values since charts aren't used anywhere and weren't specced.
  - Kept every shadcn semantic variable (`--background`, `--foreground`, `--primary`, `--card`, `--muted`, `--border`, `--input`, `--ring`, `--sidebar*`, etc.) alive as a **value alias** onto the new palette (e.g. `--primary: var(--accent-primary)`) instead of renaming classes inside `components/ui/*`. That means every shadcn primitive (Button, Card, Dialog, Input, Tabs, Textarea, ScrollArea) now renders the new palette automatically with zero hand-edits to those protected/generated files — `--primary` buttons are cyan now, `--destructive` is the new error red, etc.
  - Added the new semantic Tailwind utility keys from `ui-context.md` to the `@theme inline` block (`--color-base`, `-surface`, `-elevated`, `-subtle`, `-surface-border`, `-surface-border-subtle`, `-copy-primary/secondary/muted/faint`, `-brand`, `-accent-dim`, `-ai`, `-ai-text`, `-error/success/warning`) so `bg-base`, `text-copy-primary`, `border-surface-border`, `text-brand`, `bg-accent-dim`, etc. (the exact utility names both `ui-context.md` and `code-standards.md` call for) are real, working classes.
  - Migrated every app-owned (non-`components/ui/*`) file's explicit color classes to the new names, since `code-standards.md` requires the new utility names going forward: `app/layout.tsx` (Clerk `appearance.variables`, now pointing at `--bg-base`/`--accent-primary`/etc. directly instead of the shadcn-adapter var names), `components/auth/auth-layout.tsx`, `components/editor/editor-navbar.tsx`, `components/editor/project-sidebar.tsx`, `app/editor/page.tsx`.
  - Applied the modal radius from `ui-context.md#layout-patterns` (`rounded-3xl`) to `components/editor/editor-dialog.tsx`'s `DialogContent` via its own `className` override — that file is app-owned, not shadcn-generated, so no protected-file conflict.
  - Verified: `npm run build` and `npm run lint` clean; re-ran the Playwright screenshot pass against `/sign-in` and `/sign-up` — cyan `--accent-primary` now visibly renders on the primary button and links, near-black `--bg-base` background reads correctly, zero console errors.
- Implemented `04-project-dialogs.md` end to end:
  - `types/project.ts` (new) — `Project` interface (`id`, `name`, `slug`, `isOwner`).
  - `lib/mock-projects.ts` (new) — static `MOCK_PROJECTS` array (2 owned, 1 shared) used as the hook's initial state.
  - `lib/utils.ts` — added `slugify()` for the live slug preview.
  - Added the `dropdown-menu` shadcn component (`npx shadcn add dropdown-menu`, `components/ui/dropdown-menu.tsx`) for sidebar item actions — not hand-written, per the protected-foundation-components rule.
  - `hooks/use-project-dialogs.ts` (new) — the dedicated hook the spec calls for: owns dialog state (`{type: "create"|"rename"|"delete", project?}` | `null`), form state (`name`, derived `slug`), loading state, and in-memory create/rename/delete against the mock `projects` array (simulated `setTimeout` delay, no API calls, per spec's "no API calls or persistence").
  - `components/editor/project-dialogs.tsx` (new) — the three dialogs (Create/Rename/Delete) built on the existing `EditorDialog` pattern, driven entirely by the hook's return value. Create has the name input + live slug preview; Rename is prefilled, auto-focuses, and submits on Enter; Delete is confirmation-only with a `destructive`-variant confirm button, no input.
  - `components/editor/editor-home.tsx` (new) — the centered, card-free empty state (heading, description, `New Project` button with `Plus` icon) that now fills `app/editor/page.tsx`'s `<main>`, replacing the old "Canvas" placeholder text.
  - `components/editor/project-sidebar.tsx` — added a mobile backdrop scrim (`md:hidden`, click-to-close, opacity-transitioned) and project list items per tab (owned vs. shared, split from the single mock array by `isOwner`). Owned items get a `DropdownMenu` (kebab trigger) with Rename/Delete; shared items render no actions menu at all, per spec ("hide actions for shared/collaborator projects" — not just disabled, actually absent).
  - `app/editor/page.tsx` — now the composition root: owns `useProjectDialogs()`, wires the navbar/sidebar toggle, passes `projects`/`onCreateProject`/`onRenameProject`/`onDeleteProject` into `ProjectSidebar`, renders `EditorHome` in `<main>`, and renders `ProjectDialogs` at the bottom. Navbar and existing sidebar toggle behavior untouched, per spec.
  - Verified end to end with a real headless-browser pass (Playwright via `npx`): built a temporary unauthenticated mirror route to get past Clerk's proxy protection (added `/dev-preview` to `proxy.ts`'s public-route list, added `app/dev-preview/page.tsx` rendering the same composition as `/editor`), drove all 21 spec-derived checks (slug preview live-updates, owned-vs-shared action visibility, rename prefill/autofocus/Enter-submit, delete's destructive-only confirmation, mobile backdrop scrim + click-outside-closes) — all passed, zero console errors — then deleted the temp route and reverted `proxy.ts` back to its original two-entry public-route list (confirmed via `git diff proxy.ts` showing no changes).
  - Verified: `tsc --noEmit` clean, `npm run lint` clean, `npm run build` succeeds (`/editor` still static).
  - Follow-up per user request: Create dialog now reads "New Project" (was "Create project") at `text-xl`/`text-copy-primary` (was the shared `DialogTitle` default `text-base`, no explicit color) and "Give your project a name to get started." (was "Start a new architecture workspace."). Changed `EditorDialog`'s `title`/`description` prop types from `string` to `ReactNode` so this one dialog could override size/color via a wrapping `<span>` without touching the shared, protected `components/ui/dialog.tsx` `DialogTitle` styles that every other dialog still uses unchanged. Verified `tsc --noEmit`/`npm run lint` clean.
  - Follow-up per user request: Rename dialog title now reads "Rename Project" at the same `text-xl`/`text-copy-primary` treatment as the Create dialog. Also added `text-copy-primary` directly to the Rename dialog's `Input` — the user reported typed text was unreadable in that field; `components/ui/input.tsx` has no explicit text color (relies on inherited `color`), and something in the inherited chain was resolving to a color that didn't contrast against the dark `bg-input` fill, so this pins it explicitly rather than depending on inheritance. Only applied to this Input instance, not `input.tsx` itself (protected foundation component). Verified `tsc --noEmit`/`npm run lint` clean.
  - Follow-up per user request: applied the same `text-copy-primary` fix to the Create dialog's `Input` (same unreadable-typed-text issue, now reported there too).
  - Follow-up per user request: Delete dialog title now reads "Delete Project" (was "Delete project", matched casing to the other two dialogs) at the same `text-xl`/`text-copy-primary` treatment as Create/Rename. All three dialog titles are now visually consistent.

- Implemented `05-prisma.md` end to end:
  - `prisma/models/project.prisma` (new) — `Project` (`ownerId`, `name`, optional `description`, `status` enum `ProjectStatus` (`DRAFT` default / `ARCHIVED`), `canvasJsonPath`, timestamps, indexes on `ownerId` and `createdAt`) and `ProjectCollaborator` (`project` relation with `onDelete: Cascade`, `email`, `createdAt`, unique on `[projectId, email]`, indexes on `email` and `[projectId, createdAt]`). `cuid()` IDs on both, matching Prisma's standard default (spec didn't specify an ID scheme).
  - Discovered (empirically, by testing since this Prisma version — 7.9.1 — ships no bundled docs to consult) that Prisma 7's multi-file schema support auto-discovers `.prisma` files recursively under whatever directory `schema` points to in `prisma.config.ts`, including subdirectories like `prisma/models/`. Repointed `prisma.config.ts`'s `schema` from the single file `"prisma/schema.prisma"` to the directory `"prisma"` so the pre-existing `prisma/schema.prisma` (generator/datasource only) and the new `prisma/models/project.prisma` load together — confirmed via `prisma validate`.
  - `lib/prisma.ts` (new) — cached singleton on `globalThis` (dev-only, per the standard Next.js hot-reload pattern). Branches on `DATABASE_URL`: `prisma+postgres://` prefix → native `accelerateUrl` constructor option (confirmed by reading the generated client's own type definitions that Prisma 7's `prisma-client` generator supports `accelerateUrl` directly — no `@prisma/extension-accelerate` package needed, and none was in the spec's dependency list); otherwise → `@prisma/adapter-pg`'s `PrismaPg` adapter, per spec.
  - Ran `prisma dev --detach` to start the local Prisma Postgres dev server the existing `.env`'s `prisma+postgres://localhost:...` URL points at (it wasn't running), then `prisma migrate dev --name add_project_models` — applied cleanly, `prisma/migrations/20260816131237_add_project_models/migration.sql` created.
  - Verified: `prisma validate`, `tsc --noEmit`, `npm run lint`, and `npm run build` all clean/pass.
  - Scope note: this covers only `Project`/`ProjectCollaborator`, matching `05-prisma.md`'s scope exactly. `architecture.md`'s storage model also lists spec and task-run records living in PostgreSQL — those models don't exist yet; see Next Up.
- Fixed clean-build gap (CodeRabbit): `package.json`'s `build` script is now `prisma generate && next build`. Prisma 7 doesn't generate the client during `npm install`, and `app/generated/prisma` — which `lib/prisma.ts` imports from — is gitignored/untracked, so a fresh checkout's `npm install && npm run build` would fail before this. Verified: `npm run build` regenerates the client then builds clean.

- Implemented `06-project-apis.md` end to end (backend-only, per spec — UI still uses `04-project-dialogs.md`'s mocks):
  - `app/api/projects/route.ts` (new) — `GET` lists the authenticated user's owned projects (`ownerId = userId`, newest first); `POST` creates a project owned by the authenticated user, defaulting a missing/blank `name` to `"Untitled Project"`. IDs use the schema's existing `cuid()` default — no sequential IDs added.
  - `app/api/projects/[projectId]/route.ts` (new) — `PATCH` renames (requires a non-empty `name`, 400 otherwise); `DELETE` removes the project. Both require ownership: 404 if the project doesn't exist, 403 if the authenticated user isn't the owner.
  - `lib/project-access.ts` (new) — `authorizeProjectOwner(projectId, userId)` shared by `PATCH`/`DELETE`, returns a discriminated `{status: "ok"|"not_found"|"forbidden"}` result so both routes apply the same ownership check without duplicating the Prisma lookup.
  - Every route calls `auth()` (non-throwing) directly and returns `401` JSON itself rather than depending on `proxy.ts`'s middleware-level `auth.protect()` — see Architecture Decisions for why this was necessary, not just a style choice.
  - Verified: `tsc --noEmit`, `npm run lint`, `npm run build` all clean (routes show as `ƒ` dynamic, as expected for handlers that call `auth()`).
  - Verified live end to end with a real signed-in session: user signed in manually in a Playwright-controlled Chromium window (`localhost:3000`), then every route was exercised via in-page `fetch()` (so requests carried real Clerk session cookies) — unauthenticated 401 on all four endpoints; authenticated `GET` (empty list, then populated, newest-first), `POST` with no name (defaults to `"Untitled Project"`), `POST` with a padded name (trimmed), `PATCH` rename (200), `PATCH` with a blank name (400 `"Name is required"`), `PATCH`/`DELETE` on a nonexistent ID (404), `DELETE` (204), and `PATCH`/`DELETE` against a project seeded with a different `ownerId` (403 on both) — all matched expected status/body. Seeded/cleaned up test rows via a scratch script, not committed.
  - While setting up this test, found `.env`'s `DATABASE_URL` had been silently rewritten (by some local tool/extension, not by this session) from the local `prisma dev` server to a **remote** hosted Prisma Postgres URL with a live secret key embedded — surfaced to the user immediately rather than testing against an unknown remote database; user confirmed pointing back at local. `.env` is gitignored, so this was never at risk of being committed, but flagging in case the same silent rewrite happens again — see Open Questions.

## In Progress

- None.

## Next Up

- Add Prisma models for spec records and task-run records (per `architecture.md`'s storage model) once spec generation and background-task work is specced — not yet covered by `05-prisma.md`.
- Custom node/edge rendering for the canvas (node color palette, shapes, edge markers per `ui-context.md#canvas`) — explicitly out of scope for `11-base-canvas.md`, which only wires the generic React Flow node/edge types.
- Canvas persistence/snapshotting beyond Liveblocks' own live Storage sync (`architecture.md`'s Vercel Blob snapshot model) — explicitly out of scope for `11-base-canvas.md`.
- A toolbar/controls UI for the canvas (`11-base-canvas.md` explicitly says not to add controls yet).
- Connection `<Handle>`s on `canvasNode` — still not added (see `12-shape-panel.md`'s Completed entry); shape-specific visuals themselves (diamond/hexagon/cylinder as SVGs, circle/pill via CSS) were added as a same-session follow-up after user testing surfaced the gap.
- The node color palette (`NODE_COLORS`, all 8 values) isn't selectable anywhere yet — `12-shape-panel.md`'s dropped nodes are hardcoded to `color: "default"`. Needs its own spec (a color picker on select, most likely) rather than being invented here.
- A full authenticated live pass of the canvas (real Liveblocks room, live cursors, `ErrorBoundary`/`ClientSideSuspense` fallbacks, and the shape panel/drag-drop against a real synced room instead of local state) — blocked only on an interactive human to complete Clerk OAuth sign-in in a real session, now that `LIVEBLOCKS_SECRET_KEY` is set (see Open Questions — this was the last blocker).

## Completed (continued)

- Implemented `07-wire-editor-home.md` end to end:
  - `lib/projects.ts` (new) — the "project data helper" the spec calls for. `getProjectsForUser(userId)` returns `{ owned, shared }`: owned via `prisma.project.findMany({ where: { ownerId } })`, shared via `prisma.projectCollaborator.findMany({ where: { email } })` joined to `project`, where `email` is the authenticated user's current verified email from Clerk's `currentUser()` (never client-supplied). `getProjectForUser(userId, projectId)` does the same owner-or-collaborator check for a single project (used by the new workspace route), returning a discriminated `{status: "ok"|"not_found"}` result — `not_found` is returned for both "doesn't exist" and "exists but you're not a member," so the route can 404 without leaking existence to non-members.
  - `app/editor/page.tsx` — now a server component (per spec): calls `auth()`, calls `getProjectsForUser`, renders `<EditorShell ownedProjects shared Projects />` with no `activeProject`. No client-side fetching for the initial project list, per spec.
  - `app/editor/[projectId]/page.tsx` (new) — the "workspace" route the spec's Create/Delete bullets require but don't fully define (see Architecture Decisions). Server component: resolves `params`, calls `getProjectForUser`, `notFound()`s if the user has no access, otherwise renders `EditorShell` with `activeProject` set.
  - `components/editor/editor-shell.tsx` (new) — the client component that used to be the body of `app/editor/page.tsx`. Owns sidebar open/close state and `useProjectActions()`; renders `EditorNavbar` + `ProjectSidebar` (fed `[...ownedProjects, ...sharedProjects]` — `ProjectSidebar` already splits by `project.isOwner` internally, unchanged) + `ProjectDialogs`, and switches `<main>` between `EditorHome` (no active project) and the new `EditorWorkspace` placeholder (active project set).
  - `components/editor/editor-workspace.tsx` (new) — minimal placeholder (`{project.name}` + "Canvas workspace coming soon.") for the active-workspace route, mirroring how `EditorHome` was deliberately left minimal in `04-project-dialogs.md`. Intentionally does not build any canvas — that's out of scope until a canvas spec exists.
  - `hooks/use-project-actions.ts` (new, replaces `hooks/use-project-dialogs.ts`, deleted) — renamed to match the spec's own `Use Project Actions` heading. Dialog/name state is unchanged from the old hook; `submitCreate`/`submitRename`/`submitDelete` now call the real API (`fetch` to `/api/projects` and `/api/projects/[id]`) instead of mutating an in-memory mock array. No longer holds a `projects` array in state at all — the project list's source of truth is the server-rendered props (`ownedProjects`/`sharedProjects`), and mutations trigger either `router.push()` (create → new workspace; delete of the active workspace → `/editor`) or `router.refresh()` (rename; delete of a non-active project) to re-pull fresh server data, exactly matching the spec's "refresh on success" / "otherwise refresh" wording. Added a `roomId` value (`slugify(name)-shortSuffix`, suffix generated once per dialog-open via new `lib/utils.ts#generateShortSuffix`) and a plain `error` string surfaced in the dialogs on a failed request (not specced explicitly, but silently swallowing a failed mutation would be a correctness bug, not a feature).
  - `lib/mock-projects.ts` — deleted (no longer referenced anywhere).
  - `types/project.ts` — dropped the `slug` field. It was mock-only: nothing in the DB schema or any component ever read `project.slug` (confirmed via repo-wide grep before removing), and the room-ID concept introduced by this spec is now a create-time-only derived value (`roomId` in the hook), not a persisted per-project field.
  - `components/editor/project-dialogs.tsx` — `slug` prop renamed to `roomId`; type now `Pick<UseProjectActionsReturn, ...>` (was `UseProjectDialogsReturn`); added `error` display (plain `text-error` line) to all three dialogs.
  - `app/api/projects/route.ts` — `POST` now accepts an optional `id` field in the request body (validated against a lowercase-slug pattern, 400 if present-but-invalid) and uses it as the Prisma `create` `id` instead of the schema's `cuid()` default when provided; a `P2002` unique-constraint collision returns `409`. Required to satisfy the spec's explicit "the project ID and Liveblocks room ID should stay aligned" — see Architecture Decisions for why this couldn't be done purely client-side.
  - Verified: `tsc --noEmit`, `npm run lint`, `npm run build` all clean (`npm run build` shows `/editor` and `/editor/[projectId]` as new dynamic (`ƒ`) routes, `/api/projects` and `/api/projects/[projectId]` unchanged).
  - Verified unauthenticated behavior against the running dev server via `curl`: both `/editor` and `/editor/[projectId]` return `307` to `/sign-in`, matching existing proxy-level protection — no route-level regression.
  - Verified live end to end with a real signed-in session (user signed in manually in a Playwright-controlled, visible Chromium window driven via a small file-based command bridge, since the sandbox blocks headless auth bypasses like `clerk impersonate`): sidebar opens to a real "No projects yet" empty state (no mock data); Create dialog's room-ID preview live-updates from `project-<suffix>` to `checkout-service-<suffix>` as the name is typed, keeping the same suffix; submitting Create navigated to `/editor/checkout-service-<suffix>` — confirming the project's database ID and the previewed room ID are the same value; the new project appeared in "My Projects" and the workspace placeholder rendered its name; Rename dialog pre-filled the current name, autofocused (`document.activeElement.id` confirmed), and Enter-to-submit updated both the sidebar item and the open workspace's heading via `router.refresh()`; Delete showed the destructive-only confirmation with the project name interpolated, and deleting the *active* workspace correctly redirected to `/editor` (`router.push`) and left the sidebar back at "No projects yet". Zero console errors from app code (one pre-existing, unrelated 404 present from the very first page load, traced to Clerk's own infrastructure requests, not this feature). Screenshots + driver scripts left in the session scratchpad only, not committed.
- Addressed four CodeRabbit findings on the `07-wire-editor-home.md` PR (two others — an architecture.md atomic-claim wording issue and a "spec file wording is stale" suggestion — were reviewed and deliberately skipped; see Architecture Decisions):
  - `hooks/use-project-actions.ts` — all three `submit*` functions now wrap their `fetch`/`.json()` calls in `try/catch/finally`. Previously an uncaught network failure (offline, dev-server hiccup, non-JSON error body) left `isLoading` stuck `true` forever, and since `close()` itself bails out early `if (isLoading) return`, the dialog became permanently stuck with no error shown and no way to dismiss it. `finally` now always resets `isLoading`.
  - `hooks/use-project-actions.ts` — `submitCreate` now regenerates `suffix` specifically on a `409` (room-ID collision), derives the replacement room ID locally, and automatically retries the POST once. A failed retry still uses the existing error path; a successful retry navigates with the server-returned project ID. Astronomically unlikely to trigger (6-char base36 suffix), but the original branch only updated state and returned without retrying.
  - `lib/projects.ts` — `getVerifiedEmail` now requires `primaryEmailAddress.verification?.status === "verified"` before trusting the email, instead of trusting whatever `primaryEmailAddress` returns regardless of verification state. Clerk's own types (`EmailAddress.verification: Verification | null`) confirm this can be `null` or non-`"verified"`; without the check, an unverified email on the requester's account could match a `ProjectCollaborator` row and leak that project's existence/name to someone who never verified owning that address.
  - `context/progress-tracker.md` — merged two duplicate `## Open Questions` headings (a pre-existing structural artifact, not introduced by this session) into one section.
  - Verified: `tsc --noEmit`, `npm run lint`, `npm run build` all clean.

- Implemented `08-editor-workspace-shell.md` end to end:
  - `app/editor/[projectId]/page.tsx` → renamed to `app/editor/[roomId]/page.tsx` (`git mv`) — the spec names the dynamic segment `roomId`; `07-wire-editor-home.md`'s Architecture Decisions already established the DB project ID and the future Liveblocks room ID are the same value, so this is a pure rename with no behavior change (URL shape is identical, only the internal param name changes). The page returns `<AccessDenied />` directly instead of calling `notFound()` when `getProjectForUser` reports `"not_found"` — covers both "no access" and "doesn't exist" in one path, matching the spec's "non-existent projects also show `AccessDenied`" (the existing `getProjectForUser` already collapses both cases into `"not_found"` so as not to leak project existence to non-members, per `06`/`07`'s access design).
  - `components/editor/access-denied.tsx` (new) — centered full-height layout, `Lock` icon (`size-8`, matches `ui-context.md`'s empty-state icon sizing), short message, link back to `/editor`.
  - `lib/project-access.ts` — extended, and refactored so identity/access-check logic lives in exactly one place (per `code-standards.md`'s "keep modules small and single-purpose"): added `getCurrentIdentity(userId)` (Clerk `currentUser()` → `{ userId, email }`, email only populated if the primary address is Clerk-verified) and `checkProjectAccess(projectId, identity)` (owner-or-collaborator check, returns `{status: "ok", project, isOwner} | {status: "not_found"}`). Kept the existing `authorizeProjectOwner` (owner-only, used by the `06`-era API mutation routes) unchanged in behavior.
  - `lib/projects.ts` — `getProjectForUser`/`getProjectsForUser` now call `getCurrentIdentity`/`checkProjectAccess` from `lib/project-access.ts` instead of each independently re-deriving the verified email (that logic — `getVerifiedEmail` — was duplicated across both files before this pass; now lives once, in `project-access.ts`). No behavior change; return shapes unchanged.
  - `components/editor/editor-navbar.tsx` — added optional `projectName` (rendered centered when present), and optional `isAiSidebarOpen`/`onToggleAiSidebar` — when the latter is provided, a `Share2` icon button (no click behavior — sharing is explicitly out of scope per spec) and a `Sparkles` icon toggle button (drives the new right AI panel, highlighted via the `secondary` button variant when open) render on the right, before `UserButton`. Both are only shown when there's an active project — the plain `/editor` home screen is unaffected.
  - `components/editor/project-sidebar.tsx` — added `activeProjectId` prop, threaded down to `ProjectItem` as `isActive`; active row gets `bg-accent-dim text-brand` per the existing accent tokens (no new tokens invented).
  - `components/editor/editor-workspace.tsx` — rewritten: dropped the old inline `<h1>{project.name}</h1>` (now shown in the navbar instead, per spec), canvas placeholder is now `flex-1 items-center justify-center bg-base` with only the "Canvas workspace coming soon." message, and it renders the new right-side AI sidebar placeholder (`w-80`, "AI chat coming soon.") when `isAiSidebarOpen` is true, so the canvas area fills the remaining space either way.
  - `components/editor/editor-shell.tsx` — added `isAiSidebarOpen` state (lifted here, same pattern as the existing `isSidebarOpen`), wires it into `EditorNavbar`'s toggle and `EditorWorkspace`'s panel, and passes `activeProject?.id` into `ProjectSidebar` for the active-row highlight.
  - Verified: `tsc --noEmit`, `npm run lint`, `npm run build` all clean (`npm run build` shows `/editor/[roomId]` as the new dynamic route, replacing the old `/editor/[projectId]` entry; regenerated `.next` from scratch first since Next's route-type generation had a stale reference to the deleted folder).
  - Verified unauthenticated behavior via `curl`: `/` and `/editor/<any-id>` both still `307` to `/sign-in` — no regression to `proxy.ts`-level protection from the folder rename.
  - Verified live end to end with a real signed-in session (user completed GitHub OAuth sign-in manually in a visible, Playwright-controlled Chromium window; the resulting `storageState` was then reused by a separate headless Playwright driver for the actual interaction pass — same "manual sign-in, scripted drive" pattern as prior sessions, adapted since the OAuth roundtrip meant the naive "wait until URL leaves `/sign-in`" check from earlier work needed fixing to instead wait for landing back on `localhost:3000` *without* `/sign-in`/`/sign-up` in the URL): created a real project from the sidebar ("Workspace Shell Test"), landed on `/editor/workspace-shell-test-<suffix>` — navbar centered the project name, `Share` and `Sparkles` (AI toggle) icons rendered next to `UserButton`; clicking the AI toggle opened the right placeholder panel ("AI chat coming soon.") without disturbing the canvas placeholder ("Canvas workspace coming soon.", centered, dark `bg-base`); opening the project sidebar showed the active project's row highlighted in the brand accent color (`bg-accent-dim`/`text-brand`) distinct from the rest of the list; navigating to `/editor/<a-nonexistent-room-id>` rendered the new `AccessDenied` screen (lock icon, message, working "Back to editor" link) instead of a 404. Zero console/page errors throughout. Deleted the test project afterward via the UI's own delete flow to leave the database clean; screenshots and driver scripts were scratchpad-only, not committed.
  - Follow-up per user request, with a reference screenshot of the target visual design: gave the workspace shell a full cosmetic pass (still no new logic — every element is still static copy/placeholder, per spec's "no real canvas logic yet"):
    - `components/editor/editor-navbar.tsx` — `h-12` → `h-16` and switched from the 3-column `flex-1` layout to `justify-between`; project name is now left-aligned next to the sidebar toggle as a two-line stack (`{name}` / "Workspace" subtitle) instead of centered single-line. `Share` and the AI toggle are now labeled pill buttons (`rounded-full`, icon + text) instead of icon-only — `Share` uses `variant="outline"`, the AI toggle switches `outline` → `default` (solid brand-cyan pill, text auto-contrasts via the existing `--primary-foreground: var(--bg-base)` alias) when open, matching the reference's "active tool" treatment.
    - `components/editor/project-sidebar.tsx` — updated its hardcoded `top-12`/`h-[calc(100%-3rem)]` offsets to `top-16`/`h-[calc(100%-4rem)]` to match the new navbar height (grepped the rest of `components`/`app` first to confirm no other file hardcoded the old `h-12`/`3rem` navbar height). Active project row now also gets a small brand-colored dot marker before the name (in addition to the existing tint), and "New Project" is now `rounded-full` to match the pill language used elsewhere.
    - `components/editor/editor-workspace.tsx` — rewritten again: canvas placeholder is no longer a single line of text — it's a layered background (a faint CSS-grid pattern masked to a radial fade, plus a low-opacity brand-color radial glow, both built from `var(--border-default)`/`var(--accent-primary)` via inline `style` so no hex is hardcoded) behind a centered icon tile (`Compass`, in a `bg-accent-dim`/`text-brand` rounded-2xl tile), an uppercase eyebrow ("Workspace shell"), a real heading, and a descriptive paragraph — copy provided by the user's reference design, not invented. The AI panel gained a proper header (title + "Placeholder panel" subtitle + small `Sparkles` glyph), a "Chat surface pending" card (icon avatar in `bg-ai/20`/`text-ai-text`, title, description), and a "Future hooks" section pinned to the bottom via `mt-auto`.
    - Verified: `tsc --noEmit`/`npm run lint` clean; live signed-in Playwright pass (same manual-sign-in-then-reuse-storageState pattern) screenshotted the redesigned workspace with the AI panel open — visual match confirmed against the user's reference image, zero console errors. Cleaned up the test project(s) and scratchpad artifacts afterward.
    - Not addressed by this pass: `context/ui-context.md`'s Layout Patterns line ("Sidebars: floating overlay with dark semi-transparent background") is still not true of `project-sidebar.tsx` (solid `bg-surface`) — same pre-existing gap noted below under "Sidebar isn't semi-transparent," untouched here since the reference screenshot didn't call for it either.
  - Follow-up per user request: the left `ProjectSidebar` is `fixed`-positioned (an intentional choice from the original `08` pass — see Architecture Decisions — so it never shifts on mobile, has its own slide transition, and stays clear of the flex layout), but that meant it only ever floated *over* the canvas rather than making room for it — unlike the right AI panel, which is a real flex sibling of the canvas and already pushed it. So opening the project sidebar didn't re-center the canvas/AI-panel content the way opening the AI panel did, and the user asked for both panels to behave the same way: canvas content should always be centered in whatever space is left between whichever panels are open.
    - `components/editor/editor-shell.tsx` — `<main>` now takes `md:ml-72` (matching the sidebar's own `w-72`) when `isSidebarOpen`, with `transition-[margin-left] duration-200 ease-in-out` running in sync with the sidebar's existing slide transition. Below `md` the sidebar still behaves as a pure overlay (with its existing backdrop scrim) and `<main>` doesn't shift — pushing a 288px margin on a phone-width viewport would just squeeze the canvas, and the backdrop already signals "temporary layer on top" there. Both `EditorHome` and `EditorWorkspace`'s canvas pane were already self-centering (`flex-1 items-center justify-center`), so this one change re-centers both screens with no changes needed inside either component.
    - Verified: `tsc --noEmit`/`npm run lint` clean. Live signed-in Playwright pass measured the horizontal center of the canvas heading at 1440px width across all four panel combinations and confirmed exact midpoints between whichever edges are open (both closed → 720 = center of 1440; left only → 864 = midpoint of 288–1440; both open → 704 = midpoint of 288–1120; right only → 560 = midpoint of 0–1120), zero console errors. Cleaned up test projects and scratchpad artifacts afterward.
    - Updated the original `fixed` panel note earlier in this file's Completed section (it used to claim the sidebar "never pushes canvas content" — now only true below `md`).
  - Bug fix reported by the user: after opening a project, navigating back out to `/editor`, and trying to click that same project again from the sidebar, nothing happened. Root cause: `ProjectItem` in `components/editor/project-sidebar.tsx` never had a click-to-open handler in the first place — checked `07-wire-editor-home.md` (the spec that wired the sidebar to real data) and it only specifies Create/Rename/Delete, never "click a row to open the project." Not a regression from a recent change; a genuine gap across every spec so far, since the only existing way into a workspace was via the Create flow's own `router.push`.
    - `components/editor/project-sidebar.tsx` — wrapped the name/dot portion of each `ProjectItem` row in a `next/link` `Link` to `/editor/{project.id}`, sized to fill the row next to (not inside) the existing actions dropdown so the two don't nest or conflict.
    - Verified: `tsc --noEmit`/`npm run lint` clean. Live signed-in Playwright pass reproduced the user's exact repro (create a project → navigate back to `/editor` → click it again in the sidebar) and confirmed it now lands back on the same workspace URL with the navbar showing the project name, zero console errors.

- Implemented `09-share-dialog.md`:
  - `app/api/projects/[projectId]/collaborators/route.ts` (new) — authenticated `GET` lists collaborators for owners and members; `POST` validates and normalizes an email, requires project ownership, creates the `ProjectCollaborator`, and returns `409` for duplicate access. Unauthorized, missing, and forbidden cases use the existing API response conventions.
  - `app/api/projects/[projectId]/collaborators/[collaboratorId]/route.ts` (new) — owner-only `DELETE`; scopes the delete by both project and collaborator ID so an owner cannot remove a record from another project, returning `404` when the scoped record does not exist.
  - `lib/project-collaborators.ts` and `types/collaborator.ts` (new) — database collaborator records are enriched through Clerk Backend API user lookups batched at the SDK's 100-email limit. Returned users are exact-matched only through their normalized, Clerk-verified primary email—the same eligibility rule used by `checkProjectAccess`—and expose display name/avatar data; secondary/unverified addresses, missing Clerk users, or a temporarily unavailable Clerk API fall back to the stored email without blocking access-list rendering. No local user table was added.
  - `components/ui/avatar.tsx` (new) — generated with `npx shadcn add avatar -y`, preserving the protected-foundation-component workflow.
  - `components/editor/share-dialog.tsx` (new) — opens from the active workspace navbar, loads the collaborator list, and renders Clerk names/avatars where available. Owners get the invite form, remove controls, and a copy-link control with two-second `Copied!` feedback; collaborators receive the list-only UI with all management and copy-link controls absent. Request failures are surfaced inline and async controls expose loading states.
  - `components/editor/editor-navbar.tsx` / `components/editor/editor-shell.tsx` — the existing Share pill now opens the controlled dialog for the active project; no Share action appears on `/editor` without an active workspace.
  - Permission enforcement is duplicated intentionally across boundaries: `Project.isOwner` controls which UI is rendered, while both mutation routes independently call `authorizeProjectOwner`, so client-side prop or request tampering cannot grant management access.
  - Verified: `npx tsc --noEmit` and `npm run lint` pass. `npm run build -- --webpack` (the documented Next.js 16 webpack build path) compiles, type-checks, prerenders, and lists both new dynamic routes successfully. The default `npm run build` was attempted twice, including with build-only sandbox escalation, but Turbopack panicked before compilation because this host denied its CSS worker permission to bind an internal port (`Operation not permitted`); no application/type/CSS diagnostic was emitted.
  - Follow-up visual pass from the user's share-dialog references:
    - `components/editor/share-dialog.tsx` — widened the modal and reorganized it into the reference's visual hierarchy: bordered workspace-link and invite panels, larger individual member cards, right-aligned counts, profile avatars, uppercase role pills, and clearer vertical spacing. Existing functional copy, loading/error behavior, owner-only controls, and temporary `Copied!` feedback remain intact.
    - The access list now includes the Clerk-enriched project owner with an `OWNER` role, groups Clerk-resolved collaborator records under `People with access` with `COLLABORATOR` roles, and separates unresolved email records under `Invited people` with `INVITED` roles. Owners can remove either collaborator group; collaborators still get a read-only list with all management controls absent. No schema or local-user-table change was needed: invite state is derived from whether the stored collaborator email resolves to an existing Clerk user.
    - `lib/project-collaborators.ts`, `types/collaborator.ts`, and the collaborator `GET` route — added owner enrichment and the derived active/invited status returned to the UI. Clerk lookup failure still degrades safely to email-only rendering.
    - `components/editor/editor-dialog.tsx` — added an optional app-level `contentClassName` passthrough so the Share dialog can use its wider, scroll-safe layout without editing the protected generated `components/ui/dialog.tsx` primitive or changing the existing project dialogs.
    - Verified after the visual pass: `npx tsc --noEmit`, `npm run lint`, `git diff --check`, and `npm run build -- --webpack` all pass.
  - Review fix: the collaborator-loading effect now resets loading and clears stale errors whenever the dialog opens, so subsequent opens show the correct request state while refreshing. Verified with `npx tsc --noEmit`, `npm run lint`, and `git diff --check`.

- Implemented `10-liveblocks-set.md` end to end:
  - `liveblocks.config.ts` — filled in the pre-existing boilerplate's `Presence` (`cursor: {x, y} | null`, `isThinking: boolean`) and `UserMeta` (`id`, `info: {name, avatar, color}`) per spec. Also converted the still-unused placeholder types (`Storage`, `RoomEvent`, `ThreadMetadata`, `RoomInfo`) from bare `{}` to `Record<string, never>` — the boilerplate's own `{}` shape failed this repo's `@typescript-eslint/no-empty-object-type` lint rule, which the boilerplate (presumably scaffolded by a Liveblocks CLI/template) was never checked against here; left genuinely empty since nothing beyond Presence/UserMeta was in scope.
  - `lib/liveblocks.ts` (new) — `getLiveblocksClient()`, a `globalThis`-cached singleton (same dev-hot-reload-safe pattern as `lib/prisma.ts`) wrapping `@liveblocks/node`'s `Liveblocks` client. Deliberately constructed **lazily inside the getter**, not at module top level — see Architecture Decisions for why. Also exports `getCursorColorForUser(userId)`, a deterministic `hash(userId) % 7` mapping into a fixed 7-color palette copied verbatim from `app/globals.css`'s `--node-*-text` values (blue/purple/orange/red/pink/green/teal) — those are already the project's vivid, dark-canvas-readable palette, so this reuses the existing design language instead of inventing a new one for cursors.
  - `app/api/liveblocks-auth/route.ts` (new) — `POST` handler: `auth()` first (401 if unauthenticated), parses `{ room: string }` from the body (400 if missing/malformed), resolves identity via the existing `getCurrentIdentity`/`checkProjectAccess` from `lib/project-access.ts` (403 for any non-`"ok"` result — collapses "doesn't exist" and "not a member" into one response, consistent with that helper's existing non-leaking design), then `getOrCreateRoom(projectId, { defaultAccesses: ["room:write"] })` (room ID = project ID, per spec), then `liveblocks.identifyUser({userId, groupIds: []}, {userInfo: {name, avatar, color}})` using Clerk's `currentUser()` for name/avatar and the new cursor-color helper, returning the SDK's own `{status, body}` directly as the response.
  - Dependency gap found and fixed: the spec states "All required Liveblocks packages are already installed," but `@liveblocks/node` — the server-side SDK this route needs for `Liveblocks`/`identifyUser`/`getOrCreateRoom` — was not in `package.json` (only `client`/`react`/`react-flow`/`react-ui` were). Installed it (`npm install @liveblocks/node`, resolved to `^3.24.0`, matching the other Liveblocks packages' pinned major).
  - Verified: `npx tsc --noEmit`, `npm run lint` clean. Both `npm run build -- --webpack` and the default `npm run build` (Turbopack — the sandbox issue noted in `09-share-dialog.md`'s Completed entry did not reproduce this session) pass, both listing `/api/liveblocks-auth` as a new dynamic route.
  - Verified live against the running dev server: unauthenticated `POST /api/liveblocks-auth` (with and without a body) returns `401` via `curl`, confirming the auth gate runs before any Liveblocks SDK call.

- Implemented `12-shape-panel.md` end to end:
  - `types/canvas.ts` — added `SHAPE_DRAG_MIME_TYPE` (a dedicated `"application/x-ghost-shape"` MIME key, not generic `"application/json"`, so drop handling can safely ignore any unrelated drag payload), `ShapeDragPayload` (`{shape, width, height}`), and `DEFAULT_SHAPE_SIZE` — a `Record<NodeShape, {width, height}>` with sensible per-shape defaults (`rectangle` 160×80 wider-than-tall, `circle` 120×120 square, `diamond` 160×160 square and larger than circle "so labels have room" per spec, `pill` 160×60, `cylinder` 120×110, `hexagon` 170×90 — the last three weren't given explicit constraints by the spec, sized by the same "sensible default" judgment call).
  - `components/editor/shape-panel.tsx` (new) — the floating pill-shaped toolbar: `rounded-full` row of 6 draggable icon buttons (`RectangleHorizontal`/`Diamond`/`Circle`/`Pill`/`Cylinder`/`Hexagon` from `lucide-react`, matching the 6 `NODE_SHAPES` values). Each button's `onDragStart` looks up its shape's `DEFAULT_SHAPE_SIZE` and writes a `ShapeDragPayload` JSON string onto `dataTransfer` under `SHAPE_DRAG_MIME_TYPE`.
  - `components/editor/canvas-node.tsx` (new) — the "basic renderer" the spec calls for: every node of type `"canvasNode"` renders as one bordered, centered-label `div` regardless of its `data.shape` value (per spec's explicit "shape-specific visuals will be added later" scope limit), sized from the node's own `width`/`height`, colored via inline `style` referencing `var(--node-{color}-fill/text)` (no Tailwind utility exists for these tokens, so this follows the same inline-`var()` pattern already used elsewhere for un-aliased tokens, per `code-standards.md`'s "reference CSS custom properties ... no exceptions for non-UI code"). Deliberately has no connection `<Handle>` elements — out of scope for this unit's literal "simple bordered rectangle with the label centered" instruction; connectivity for these nodes is left for whichever future spec adds shape-specific visuals.
  - `components/editor/canvas.tsx` — restructured: `Canvas` now wraps a new inner `CanvasContent` in `@xyflow/react`'s `ReactFlowProvider`, since `screenToFlowPosition` (from `useReactFlow()`) is only available to a *descendant* of that provider, not a sibling of `<ReactFlow>` — confirmed via `node_modules/@xyflow/react`'s own `.d.ts` files that `<ReactFlow>` doesn't implicitly provide this context to elements outside itself. `CanvasContent` renders the existing `<ReactFlow>` tree unchanged except for two additions: `nodeTypes={{ canvasNode: CanvasNodeRenderer }}` (module-level constant, not re-created per render) and a new `<Panel position="bottom-center"><ShapePanel /></Panel>` child (React Flow's own `Panel` primitive, used exactly as its own docs example shows, for the floating toolbar positioning). The outer wrapper `div` (already existing, now also holding the provider-scoped drag handlers) gets `onDragOver` (calls `preventDefault`, sets `dropEffect = "move"`) and `onDrop`: reads the `SHAPE_DRAG_MIME_TYPE` payload off `dataTransfer` (silently no-ops if absent, e.g. some unrelated drag), converts the pointer position via `screenToFlowPosition`, and dispatches `onNodesChange([{type: "add", item: newNode}])` — the same Liveblocks-synced `onNodesChange` callback already used for all other node mutations, so a dropped node is created through the identical synced pathway as every other change (confirmed via `@xyflow/system`'s `.d.ts` that `{type: "add", item}` is a real, documented `NodeChange` variant, not an invented shortcut). New node ID is `${shape}-${Date.now()}-${counter}` where `counter` is a `useRef`-backed value incremented once per drop within that client's `CanvasContent` instance (per spec's "shape name, timestamp, and a counter" — a per-session counter was sufficient since the spec didn't call for cross-client uniqueness guarantees beyond what the timestamp already provides).
  - Verified: `npx tsc --noEmit`, `npm run lint`, and `npm run build` all clean (no route changes — this spec touches no `app/` routes).
  - Verified live in a real browser (Playwright, headless): since `.env.local` now has a real `LIVEBLOCKS_SECRET_KEY` (see below — this was the long-standing blocker on `10`/`11`'s live verification, now resolved), a **temporary** unauthenticated test route (`app/dev-canvas-test/page.tsx`, `/dev-canvas-test` added to `proxy.ts`'s public-route list) was added to exercise the new drag/drop + rendering logic directly — reusing the actual `ShapePanel`/`CanvasNodeRenderer` components with local `useState` standing in for `useLiveblocksFlow` (same `NodeChange`/`onNodesChange` contract, so the exercised code path is identical to production; Liveblocks auth itself wasn't in scope for this spec). Confirmed: all 6 shape buttons render in the bottom-center pill; simulated HTML5 drag-and-drop (native Playwright drag doesn't reliably carry custom-MIME `dataTransfer` payloads, so drag/dragover/drop `DragEvent`s were dispatched directly) for each of the 6 shapes produced a node with the exact expected `id` pattern, `type: "canvasNode"`, correct per-shape `width`/`height` from `DEFAULT_SHAPE_SIZE`, `data: {label: "", color: "default", shape: <dragged shape>}`, and a screen-to-flow-converted `position`; visually, each node rendered as a bordered rounded rectangle at the drop point, appeared in the `MiniMap`, and the counter suffix incremented correctly across sequential drops (`-1` through `-5` in one run). Zero console/page errors. Deleted `app/dev-canvas-test/` and reverted `proxy.ts` afterward (confirmed via `git diff`/`git status` showing no residual changes) — same temporary-route-then-revert pattern used for live-testing `04-project-dialogs.md`.
  - **Newly discovered this session**: `.env.local` now has a real, non-placeholder `LIVEBLOCKS_SECRET_KEY` (`sk_...`, 71 chars) — this was **not** present at the start of this session and resolves the blocker noted in `10-liveblocks-set.md`/`11-base-canvas.md`'s Completed entries and the first Open Questions bullet below. Origin unknown (not set by this session). Full authenticated live verification of the canvas room connection itself (real Liveblocks room, live cursors, the `ErrorBoundary`/`ClientSideSuspense` fallbacks) still wasn't done in *this* session — this session's live test deliberately bypassed Clerk auth via the temporary route above rather than attempting a full OAuth sign-in (no interactive human available to complete it in this non-interactive session, same limitation noted in `11-base-canvas.md`) — but the key being present now means a future session with an interactive human can complete that verification without being blocked on provisioning one first.
  - User-reported follow-up, same session: dropped shapes all rendered as the same plain bordered rectangle regardless of which shape was dragged (e.g. dragging "diamond" didn't produce a diamond) — this was the literal, intentional behavior of the first pass (`12-shape-panel.md`'s own text: "render every shape as a simple bordered rectangle... Shape-specific visuals will be added later"), but the user expected the visual to match the dragged shape. Since `ui-context.md#canvas` already fully specifies the per-shape rendering contract (list of 6 shapes, "Complex shapes (diamond, hexagon, cylinder) are rendered as inline SVGs rather than CSS borders"), implementing it now used only already-documented spec content, not invented behavior, per `ai-workflow-rules.md`'s "do not invent product behavior" rule.
    - `components/editor/canvas-node.tsx` — rewritten to branch on `data.shape`: `circle`/`pill` now use CSS `rounded-full` (a square box for circle, a wide box for pill — the shape difference comes entirely from the node's own aspect ratio via `DEFAULT_SHAPE_SIZE`, not from separate code paths); `rectangle` keeps the original `rounded-xl` bordered box; `diamond`/`hexagon`/`cylinder` (the three `ui-context.md` calls "complex") now render as inline SVG (`DiamondShape`/`HexagonShape`/`CylinderShape`, each a small local component taking `width`/`height`/`fill`/`stroke`) with the label absolutely-overlaid on top via a centered `<span>`, `max-width` clamped to `60%` of the node's width so text doesn't run past the shape's narrower regions (most visibly the diamond's pointed top/bottom). `CylinderShape` draws a rounded-bottom body path plus a full top ellipse (the standard "database icon" construction — two ellipse arcs plus a front ellipse "lid") rather than a single simple path, since a single rounded-rect-style path doesn't read as a cylinder. Every shape still colors itself purely from `var(--node-{color}-fill/text)` (SVG `fill`/`stroke` props, not hardcoded colors), so `code-standards.md`'s "no literal color values, even in canvas code" still holds.
    - Verified: `npx tsc --noEmit`, `npm run lint`, `npm run build` all clean. Re-ran the same temporary-unauthenticated-route live-verification pattern used above (`app/dev-canvas-test/`, deleted afterward; `proxy.ts` diff confirmed empty again) — dropped all 6 shapes and visually confirmed each one now renders as its own distinct, correctly-labeled shape (diamond is a rotated square, circle is round, pill is a stadium shape, cylinder shows the database-style lid ellipse, hexagon is six-sided, rectangle unchanged) — zero console errors.
  - **Not verified live**: the authenticated 403 path and the full success path (room creation + session token issuance), because no `LIVEBLOCKS_SECRET_KEY` exists in `.env.local` and one can't be generated from this session — see Open Questions. The `Liveblocks` client constructor validates the secret's `sk_`-prefixed format eagerly, which is exactly what motivated the lazy-construction decision below (a missing/placeholder key no longer breaks the build, but a real key is still required for the route to actually function).

- Implemented `11-base-canvas.md` end to end:
  - `types/canvas.ts` (new) — `NODE_COLORS`/`NodeColor` (the 8 keys — `default`/`blue`/`purple`/`orange`/`red`/`pink`/`green`/`teal` — matching the `--node-*-fill`/`--node-*-text` pairs already in `app/globals.css` and named exactly as `ui-context.md#canvas` specifies: "referenced through the `NODE_COLORS` type in `types/canvas.ts`") and `NODE_SHAPES`/`NodeShape` (the 6 shapes `ui-context.md#canvas` lists — `rectangle`/`diamond`/`circle`/`pill`/`cylinder`/`hexagon`), `CanvasNodeData` (`label`/`color`/`shape`), and the `@xyflow/react`-typed `CanvasNode`/`CanvasEdge` aliases (`Node<CanvasNodeData, "canvasNode">` / `Edge<Record<string, never>, "canvasEdge">`). Per this spec's own scope limit ("don't add custom node or edge rendering yet"), these are type-only — no component registered against the `"canvasNode"`/`"canvasEdge"` literal yet, so React Flow still renders every node/edge with its built-in default renderer.
  - `components/editor/canvas.tsx` (new) — the actual React Flow surface. `useLiveblocksFlow<CanvasNode, CanvasEdge>({ suspense: true, nodes: { initial: [] }, edges: { initial: [] } })` (empty start state, per spec) feeds `nodes`/`edges`/`onNodesChange`/`onEdgesChange`/`onConnect`/`onDelete` straight into `<ReactFlow>`, with `connectionMode={ConnectionMode.Loose}` (loose connection behavior), `fitView`, `@liveblocks/react-flow`'s `<Cursors />` (live cursors, using the already-typed `Presence.cursor` field), and `@xyflow/react`'s `<Background variant={BackgroundVariant.Dots} />` + `<MiniMap />`. Imports `@xyflow/react/dist/style.css`, `@liveblocks/react-ui/styles.css`, and `@liveblocks/react-flow/styles.css` directly in this client component (confirmed via `node_modules/next/dist/docs/01-app/01-getting-started/11-css.md` that the App Router allows external stylesheet imports from any colocated component, not just the root layout — this differs from the Pages Router rule most training data defaults to).
  - `components/editor/canvas-room.tsx` (new) — the "client-side editor/canvas wrapper" the spec calls for. `LiveblocksProvider authEndpoint="/api/liveblocks-auth"` → `RoomProvider id={roomId} initialPresence={{ cursor: null, isThinking: false }}` (the `Presence` type from `10-liveblocks-set.md`'s `liveblocks.config.ts` makes `isThinking` a required field alongside the spec-mandated `cursor: null`, so it's initialized to `false`) → `ErrorBoundary` (from the newly installed `react-error-boundary`, per this project's own `liveblocks-best-practices` skill guidance) with a static "couldn't connect" fallback → `ClientSideSuspense` with a "Connecting to canvas…" fallback → `<Canvas />`. Room ID is the project ID (unchanged from `07-wire-editor-home.md`'s established project-ID-is-room-ID design).
  - `npm install react-error-boundary` (new dependency, `^6.1.3`) — required by the `ErrorBoundary` pattern above; this project's skill docs recommend it explicitly for structuring Liveblocks suspense/error fallbacks, and it wasn't already installed.
  - `components/editor/editor-workspace.tsx` — the static "Canvas and collaboration tooling land here next." placeholder pane (dot-grid CSS background, glow, icon tile, copy) is fully replaced by `<CanvasRoom roomId={project.id} />`; the AI sidebar `<aside>` and its placeholder content are untouched, per spec's "keep this focused on the collaborative canvas foundation."
  - Verified: `npx tsc --noEmit`, `npm run lint`, and the default `npm run build` (Turbopack) all pass; `/editor/[roomId]` still lists as the expected dynamic route.
  - Verified unauthenticated behavior via `curl` against the running dev server: `GET /editor/<any-room-id>` still `307`s to `/sign-in` and `POST /api/liveblocks-auth` still `401`s — confirms the new client-side room wrapper introduces no proxy/route-level regression.
  - **Not verified live (signed in)**: same blocker as `10-liveblocks-set.md` — no `LIVEBLOCKS_SECRET_KEY` in `.env.local`, so `POST /api/liveblocks-auth` 500s past the access check and the room can never actually connect. This session could not complete the OAuth sign-in step needed to drive a real browser session (no interactive human to complete it), so neither the loading skeleton, a real cursor/presence sync, nor the `ErrorBoundary`/`ClientSideSuspense` fallbacks were exercised against a live room. See Open Questions — this now blocks two features' live verification, not one.

## Open Questions

- ~~`LIVEBLOCKS_SECRET_KEY` is not set anywhere...~~ Resolved: `.env.local` now has a real `sk_...` key (found already present at the start of the `12-shape-panel.md` session; origin/who added it is unknown, not this or the prior session). `lib/liveblocks.ts#getLiveblocksClient` should no longer throw at request time. Still **not yet verified live with a real signed-in session** — no interactive human was available in the `12-shape-panel.md` session to complete Clerk OAuth, so the auth route's 403/success paths, real room connection, live cursors, and the `ErrorBoundary`/`ClientSideSuspense` fallbacks in `components/editor/canvas-room.tsx` remain unverified against a real room; only the shape-panel drag/drop logic itself was verified, against local state via a temporary unauthenticated route (see `12-shape-panel.md`'s Completed entry). This is the next thing worth verifying end to end whenever a human is available to sign in.
- `10-liveblocks-set.md` said "All required Liveblocks packages are already installed" but `@liveblocks/node` (the server SDK the auth route needs) was missing from `package.json` — installed it this session. Worth double-checking future Liveblocks-touching specs' dependency claims against `package.json` rather than trusting them, since this is the second stack-completeness gap found in this repo's specs (see the `05`/Prisma and general "most of the new stack is still not installed" note below).
- ~~`04-project-dialogs.md` didn't define a "project open" state or route...~~ Resolved by `07-wire-editor-home.md`: added `app/editor/[projectId]/page.tsx` (see Completed / Architecture Decisions). `EditorHome` now only renders when `EditorShell` has no `activeProject`.
- Spec 01-design-system.md said to match an "existing dark theme" in `global.css`, but no theme existed there (it only had `@import "tailwindcss";`). Resolved by defaulting the app to dark via a `dark` class on `<html>` rather than relying on `prefers-color-scheme`. Flag if a light/dark toggle is wanted later — this will need to move off a static class.
- 01-editor.md's spec text has a typo pointing the navbar at `componets/editor/editor-navbar.tsx` (missing "n"); implemented at the correctly spelled `components/editor/editor-navbar.tsx` to match the sidebar's path and the rest of the codebase's convention. Flag if that was actually intentional.
- ~~Spec didn't say where/how these two chrome components should be composed into an actual screen...~~ Resolved by `07-wire-editor-home.md`: the composition root moved to `components/editor/editor-shell.tsx` (client), with `app/editor/page.tsx` and `app/editor/[projectId]/page.tsx` as thin server-component hosts around it.
- ~~This entire `context/` directory lives outside the `ghost-ai` git repo...~~ Resolved: copied into `ghost-ai/context/` at the user's request, so `AGENTS.md`'s repo-relative `context/*.md` paths now resolve. The original at `~/Downloads/Six-File+Context+Methodology/templates/context/` is being kept in place intentionally, as a backup — the user confirmed not to delete it. It will not auto-sync with the in-repo copy; if the two need to match later, that's a manual re-copy.
- The Clerk sign-in/sign-up form heading reads "Sign in to **Ghose** AI" / presumably the same typo on sign-up — that's the application display name configured in the Clerk Dashboard, not something set from this codebase, so it couldn't be fixed here. Needs a rename in the Clerk Dashboard.
- ~~`project-overview.md`'s Overview, Goals, Feature scope...~~ Resolved — real product content now filled in (see Completed).
- ~~`ui-context.md`/`code-standards.md`'s design tokens don't match the actual `app/globals.css`...~~ Resolved for colors (see Completed). Two pieces intentionally deferred, not silently dropped:
  - **Radius scale isn't fully migrated.** `ui-context.md` calls for `rounded-xl` (inline/small UI), `rounded-2xl` (cards/panels), `rounded-3xl` (modals). `components/ui/card.tsx` and `components/ui/button.tsx` are shadcn-generated and still use the old proportional `--radius-*` scale (`rounded-xl`/`rounded-lg` sized off a single `--radius` base, unrelated to the new 3-tier role-based scale) — changing those class names directly would mean hand-editing protected files and would silently be wiped out by the next `npx shadcn add --overwrite`. Only fixed the one case that's app-owned, not generated: `components/editor/editor-dialog.tsx`'s `DialogContent` now gets `rounded-3xl` via its own `className` override. `Card` isn't used anywhere yet, so its `rounded-xl` (should be `rounded-2xl` per spec) has no visible impact today — whoever first uses `Card` for an actual card/panel should pass `className="rounded-2xl"` at the call site rather than editing `card.tsx`. Worth a real decision later: keep shadcn's proportional radius system as the exception, or invest in overriding it repo-wide.
  - **Sidebar isn't semi-transparent.** `ui-context.md`'s Layout Patterns says "Sidebars: floating overlay with dark semi-transparent background and subtle border" — `project-sidebar.tsx` kept its existing solid `bg-surface` rather than adding transparency/backdrop-blur, since that's a structural/visual change beyond a token rename. Flagging so it's not mistaken for done.
  - `ui-context.md`'s Canvas section (node color palette, edge style, node shapes) — `11-base-canvas.md` added the `NODE_COLORS`/`NODE_SHAPES` *types* only; `12-shape-panel.md` (see its Completed entry) registered the first real `"canvasNode"` renderer, but only as one shape-agnostic bordered rectangle and only ever with `color: "default"` — the actual per-shape SVG rendering and the rest of the 8-color palette are still unaddressed. See Next Up.
- **Most of the new stack is still not installed.** `architecture.md` specifies Prisma + PostgreSQL (now landed, see Completed) and Liveblocks + React Flow (now landed, see `11-base-canvas.md`'s Completed entry) — Trigger.dev and Vercel Blob are still missing from `package.json`. Also referenced-but-missing: `trigger/`, `data/`. None of this blocks current work, but whichever spec first needs background task execution or blob storage will need to land those as their own unit(s) per the scoping rules in `ai-workflow-rules.md`.
- `.env`'s `DATABASE_URL` was found pointing at a remote hosted Prisma Postgres database (with a live secret key inline) instead of the local `prisma dev` server, discovered mid-session with no clear origin (not a change either implementation session made). Reset back to the local connection string with the user's confirmation. Worth checking whether a Prisma VS Code extension, `prisma login`/platform link, or some other local tool is auto-managing this file — if it keeps happening, local dev testing will keep silently targeting the wrong database.
- ~~`GET /api/projects` returns only projects the user *owns*...~~ Partially resolved by `07-wire-editor-home.md`: `lib/projects.ts#getProjectsForUser` now also returns shared projects, by matching `ProjectCollaborator.email` against the authenticated user's current verified email from Clerk (`currentUser()`, never client-supplied). The `GET /api/projects` **route** itself is unchanged (still owner-only) since the editor page now reads the DB directly rather than calling its own API — nothing currently calls `GET /api/projects` except tests/external clients. Flag if that route itself should also gain shared-project listing for API consistency.
- `architecture.md`'s collaborator model describes `ProjectCollaborator` mapping emails to **immutable Clerk user IDs**, with server-side revalidation when a user's email changes. The actual `ProjectCollaborator` schema (from `05-prisma.md`) has no `userId` column — only `email`. `getProjectForUser`/`getProjectsForUser` (this session) match on the requesting user's *current* email, which is correct today but has no way to revalidate a stale email the way `architecture.md` describes, because there's nothing to revalidate against. Needs its own migration (`userId` column + backfill/revalidation logic) before `architecture.md`'s described behavior is actually true — out of scope for this UI-wiring task.

## Architecture Decisions

- Used `shadcn@latest init -d` (tool defaults: template `next`, preset `base-nova`) rather than picking a specific preset/base library, since the spec didn't specify one and the CLI's presets (nova, vega, maia, lyra, mira, luma, sera, rhea) are unfamiliar/newer than training data — deferred to the tool's own recommended default.
- Component library is `@base-ui/react` (Base UI), not Radix UI — this shadcn CLI version's default, differs from the classic shadcn/Radix setup.
- Do not hand-edit `components/ui/*` — regenerate via `npx shadcn add <component> --overwrite` instead (per AGENTS.md/spec instruction).
- Sidebar open/close and navbar's icon state are lifted to the parent (`isOpen`/`isSidebarOpen` + callback props) rather than owned internally by either component, since the navbar's icon and the sidebar's visibility must stay in sync and future chapters will need to control both from a shared editor layout.
- `proxy.ts` uses the "protected-first" `clerkMiddleware` strategy (deny by default, allow-list `/sign-in` and `/sign-up`) rather than "public-first", per spec's "Protect everything else by default." Public paths are derived from `NEXT_PUBLIC_CLERK_SIGN_IN_URL`/`NEXT_PUBLIC_CLERK_SIGN_UP_URL` instead of hardcoded strings, so renaming those routes only requires an env change.
- `proxy.ts` now treats `/api(.*)` as a public route (bypassing middleware-level `auth.protect()`), added while implementing `06-project-apis.md`. This isn't a relaxation of protection — every `app/api/*` route still calls `auth()` itself and returns `401` JSON when unauthenticated, per `code-standards.md`'s "enforce auth ... before any mutation." It was necessary, not stylistic: verified empirically (`node_modules/@clerk/nextjs/dist/esm/server/protect.js`) that `auth.protect()`'s default `handleUnauthenticated()` redirects to `/sign-in` for requests it classifies as page navigations, and otherwise calls Next's `notFound()` (a **404**, not 401) for anything it doesn't recognize as a page or a server action — a plain `fetch()`-style API request gets a 404, which directly conflicts with the spec's explicit "unauthenticated requests return 401." Route-level `auth()` checks are the only way to guarantee the 401 contract this and future `app/api/*` specs require.
- `/` still does its own explicit `auth()` check + redirect in `app/page.tsx` (rather than relying solely on the proxy's automatic redirect-to-sign-in for unauthenticated visitors) since the spec calls out both directions of that redirect explicitly and the page-level check is correct regardless of how `proxy.ts`'s matcher/public-route list evolves later.
- `07-wire-editor-home.md`'s Create bullets ("generate a short unique suffix, slugify the name to create the room ID... the project ID and Liveblocks room ID should stay aligned") only make sense if the *database* project ID and the future Liveblocks room ID are the same value — otherwise "stay aligned" would require a separate mapping the spec never describes, and there's no `roomId` column in the schema to hold one. Rather than invent a mapping, extended `POST /api/projects` (from `06-project-apis.md`) to accept an optional client-supplied `id` — the client computes `slugify(name)-<shortSuffix>` (in `hooks/use-project-actions.ts`) and sends it as `id`; the server uses it as the Prisma primary key instead of the default `cuid()` when present, and returns `409` on a (extremely unlikely, 6-char base36 suffix) collision. This is additive/backward-compatible — omitting `id` behaves exactly as `06-project-apis.md` specified. No actual Liveblocks room is created yet (the package isn't installed) — this only guarantees that whenever Liveblocks does get wired up, `project.id` is already a valid, pre-aligned room name with no lookup needed.
- Shared-project listing (`lib/projects.ts`) matches on the requesting user's own Clerk-verified email (via `currentUser()`), not a client-supplied one — consistent with `architecture.md`'s "membership checks always use the authenticated Clerk user ID (never client-supplied email)" in spirit, even though the current schema can only join on email (see Open Questions).
- Deliberately skipped two CodeRabbit findings on the `07-wire-editor-home.md` PR: (1) `architecture.md`'s canvas-snapshot/spec-publication wording describes an atomic-claim protocol for Liveblocks/Vercel Blob/Trigger.dev — none of which exist in the repo yet; fixing prose for an unbuilt subsystem now means guessing at details (blob key naming, cleanup triggers) that should come from the actual canvas spec instead. (2) `06-project-apis.md` still reads "backend-only, do not wire the UI yet" even though `07` wired it up — left as-is because numbered `context/feature-specs/*.md` files are treated as historical asks in this project (see the "Did **not** edit their prose" note earlier in Completed), not living docs; `progress-tracker.md` already carries the "this was superseded by 07" cross-reference.
- `lib/liveblocks.ts#getLiveblocksClient` constructs the `Liveblocks` client **lazily**, inside the getter, instead of as a top-level `export const` (unlike `lib/prisma.ts`'s otherwise-identical caching pattern). Discovered empirically: Next.js's build-time "Collecting page data" step imports every route module to statically analyze it, which would eagerly run `new Liveblocks({...})` at import time — and that constructor validates the secret's `sk_...` format immediately, so any build without a real `LIVEBLOCKS_SECRET_KEY` set would fail outright (confirmed: it did, until this was changed). Lazy construction means the client is only ever built on an actual incoming request, so the build no longer depends on the env var being present or well-formed. `lib/prisma.ts` doesn't have this problem because `PrismaClient`'s constructor doesn't eagerly validate/connect.
- `POST /api/liveblocks-auth` grants `defaultAccesses: ["room:write"]` when creating a room, rather than a per-user `usersAccesses` grant. Reasoning: `getOrCreateRoom` only applies its permissions config at creation time — it won't retroactively update an existing room's ACL for a collaborator added later — and the spec's own phrasing ("ensure the room exists, create only if needed") implies a check-then-create call, not an update-on-every-request one. Since this route is already the sole gate (`checkProjectAccess` runs before any token is ever issued for a room), a room-level `["room:write"]` default doesn't weaken security — nobody can reach the room without first passing this route's own membership check to get a token in the first place.
- This installed `@liveblocks/node` version's `identifyUser(identity, ...)` requires `groupIds: string[]` on the `Identity` argument (not optional, despite the skill/doc examples showing it commented out as optional) — passed `groupIds: []` since this project has no group concept. Another instance of AGENTS.md's "breaking changes vs. training data" warning; confirmed via the installed package's own `.d.ts`, not assumed.
- `components/editor/canvas.tsx` imports `@xyflow/react/dist/style.css`, `@liveblocks/react-ui/styles.css`, and `@liveblocks/react-flow/styles.css` directly at the top of the client component file, rather than in `app/layout.tsx`. Confirmed against `node_modules/next/dist/docs/01-app/01-getting-started/11-css.md` (App Router section) that "Stylesheets published by external packages can be imported anywhere in the `app` directory, including colocated components" — this is an App Router allowance that doesn't hold for the Pages Router, so it was worth verifying against this repo's actual Next.js docs rather than assuming.
- `components/editor/canvas-room.tsx` uses `react-error-boundary`'s `ErrorBoundary` (newly installed) around `ClientSideSuspense`, rather than a hand-rolled class-component error boundary or `useErrorListener`-driven state. This matches the pattern this project's own `liveblocks-best-practices` skill documents as the recommended structure (`rendering-error-components` reference) — chose the documented convention over a custom implementation since there's no reason to diverge, and `useErrorListener`'s granular per-error-code handling (auth failure vs. full room vs. no access, etc.) is more than `11-base-canvas.md`'s scope calls for; the fallback is a single static "couldn't connect" message.
- `liveblocks.config.ts`'s `Presence.isThinking` (from `10-liveblocks-set.md`) is a required, non-optional field, so `RoomProvider`'s `initialPresence` in `canvas-room.tsx` sets it to `false` alongside the spec-mandated `cursor: null` — the spec only mentions `cursor: null` because `isThinking` predates this spec and already has an established meaning from `10-liveblocks-set.md`; not a new decision, just a type-driven requirement to satisfy `RoomProviderProps`.

## Session Notes

- This Next.js project (16.3.0) ships a repo-specific `AGENTS.md` warning that APIs/conventions may differ from training data; confirmed via `node_modules/next/dist/docs/` that core CSS/Tailwind setup is unchanged, but the `shadcn` CLI itself (v4.16.2) is a materially newer generation (new preset names, `@base-ui/react` instead of Radix) — worth rechecking `npx shadcn@latest --help` before assuming behavior in future sessions.
